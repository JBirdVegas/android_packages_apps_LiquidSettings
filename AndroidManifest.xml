<?xml version="1.0" encoding="utf-8"?>
<!-- 
 * Copyright (C) 2011 The Liquid Settings Project
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.liquid.settings" android:sharedUserId="android.uid.system">

    <uses-sdk android:minSdkVersion="8" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.HARDWARE_TEST" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Liquid Settings -->
    <application android:label="@string/app_name" android:icon="@drawable/app_icon">

        <!-- Receiver -->
        <receiver android:name=".provider.NotificationReceiver" android:enabled="true">
            <intent-filter>
                <action android:name="android.app.NotificationManager.ACTION_NOTIFY" />
            </intent-filter>
        </receiver>

        <!-- Main -->
        <activity android:name=".activities.MainActivity" 
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Application -->
        <activity android:name=".activities.ApplicationActivity" 
            android:label="@string/application_title" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT" />
            </intent-filter>
        </activity>

        <!-- Phone Goggles -->
        <activity android:name=".activities.PhoneGogglesActivity">
           	<intent-filter>
              	<action android:name="android.intent.action.MAIN" />
           	</intent-filter>
        </activity>

        <activity android:name=".activities.PhoneGogglesAppActivity">
           	<intent-filter>
              	<action android:name="android.intent.action.MAIN" />
           	</intent-filter>
        </activity>

        <activity android:name=".activities.PhoneGogglesDefaultActivity">
           	<intent-filter>
              	<action android:name="android.intent.action.MAIN" />
           	</intent-filter>
        </activity>

        <activity android:name=".activities.PhoneGogglesFilterActivity">
           	<intent-filter>
              	<action android:name="android.intent.action.MAIN" />
           	</intent-filter>
        </activity>

        <!-- Behavior-->
        <activity android:name=".activities.BehaviorActivity" 
            android:label="@string/behavior_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Double Tap -->
        <activity android:name=".activities.InputDoubleTapHomeActivity"
            android:label="@string/input_double_tap_home_key_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Haptic Feedback -->
        <activity android:name=".activities.HapticTweaksActivity"
            android:label="@string/haptic_feedback_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Haptic Adjust -->
        <activity android:name=".activities.HapticAdjust"
            android:label="@string/title_haptic_menu">
            <intent-filter>
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.PREFERENCE" />
            </intent-filter>
        </activity>

        <!-- Home Key -->
        <activity android:name=".activities.LongPressHomeActivity" 
            android:label="@string/long_press_home_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Menu Key -->
        <activity android:name=".activities.LongPressMenuActivity" 
            android:label="@string/long_press_menu_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Search Key -->
        <activity android:name=".activities.InputSearchKeyActivity"
            android:label="@string/input_search_key_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- General -->
        <activity android:name=".activities.GeneralActivity" 
            android:label="@string/general_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Goodies -->
        <activity android:name=".activities.GoodiesActivity" 
            android:label="@string/goodies_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Automatic Backlight -->
        <activity android:name=".activities.BacklightActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <!-- Light Levels -->
        <activity android:configChanges="keyboard|keyboardHidden|orientation" 
            android:name=".activities.LightLevelsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <!-- Lock Activities -->
        <activity android:name=".activities.LockCustomAppActivity"
            android:label="@string/lock_custom_app_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Interface -->
        <activity android:name=".activities.UIActivity" 
            android:label="@string/ui_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Notification Activity -->
        <activity android:name=".activities.led.NotificationActivity"
            android:label="@string/trackball_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Notification Category -->    
        <activity android:name=".activities.led.CategoryActivity"
            android:label="@string/trackball_category_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Package Settings -->
        <activity android:name=".activities.led.PackageSettingsActivity"
            android:label="@string/group_applications">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Notification Advanced -->
        <activity android:name=".activities.led.AdvancedActivity"
            android:label="@string/trackball_advanced_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Power Widget -->
        <activity android:name=".activities.UIPowerWidgetActivity"
            android:label="@string/title_expanded_widget">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.PowerWidgetActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.PowerWidgetOrderActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <!-- Status Bar -->
        <activity android:name=".activities.UIStatusBarActivity"
            android:label="@string/ui_status_bar_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Color Changer -->
        <activity android:name=".activities.UIColorChangerActivity"
            android:label="@string/title_ui_tweaks">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>    
            </intent-filter>
        </activity>

        <!-- Lockscreen -->
        <activity android:name=".activities.LockscreenActivity" 
            android:label="@string/lockscreen_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Gestures -->
        <activity android:name=".activities.GestureMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.GestureCreateActivity">
            <intent-filter>
                <action android:name="android.intent.action.EDIT" />
            </intent-filter>
        </activity>

        <!-- Styles -->
        <activity android:name=".activities.LockscreenStyleActivity" 
            android:label="@string/pref_lockscreen_stylecfg_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Timeouts -->
        <activity android:name=".activities.LockscreenTimeoutActivity"
            android:label="@string/pref_lockscreen_timeout_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Unlocks -->
        <activity android:name=".activities.LockscreenUnlockActivity"
            android:label="@string/pref_lockscreen_unlock_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Widgets -->
        <activity android:name=".activities.LockscreenWidgetsActivity"
            android:label="@string/pref_lockscreen_widgets_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Overclocking -->
        <activity android:name=".activities.OverclockingActivity"
			android:label="@string/overclocking_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="com.liquid.settings.SHORTCUT" />
            </intent-filter>
        </activity>

        <!-- Performance -->
        <activity android:name=".activities.PerformanceActivity" 
            android:label="@string/performance_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Sound -->
        <activity android:name=".activities.SoundActivity"
            android:label="@string/sound_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Quiet Hours -->
        <activity android:name=".activities.SoundQuietHoursActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <!-- Tablet Addons -->
        <activity android:name=".activities.TabletActivity"
            android:label="@string/tablet_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.TabletSoftButtonsActivity"
            android:label="@string/tablet_tweaks_soft_buttons_list_head">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.TabletExtendPmActivity"
            android:label="@string/tablet_tweaks_extend_pm_list_head">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.TabletInputActivity"
            android:label="@string/tablet_tweaks_input_head">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Prop Modder -->
        <activity android:name=".activities.PropModderActivity"
            android:label="@string/propmodder_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Prop Viewer -->
        <activity android:name=".activities.PropViewerActivity"
            android:label="@string/propviewer_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Spare Parts -->
        <activity android:name=".activities.SparePartsActivity"
            android:label="@string/spareparts_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="com.liquid.settings.SHORTCUT"/>
            </intent-filter>
        </activity>

        <!-- Credits -->
        <activity android:name=".activities.CreditsActivity"
            android:label="@string/credits_title">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
            </intent-filter>
        </activity>

        <provider android:name=".provider.SettingsProvider"
            android:authorities="com.liquid.settings.provider.Settings" />
    </application>

</manifest>
