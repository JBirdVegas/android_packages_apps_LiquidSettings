<?xml version="1.0" encoding="utf-8"?>

<preference-headers xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Application -->
    <header android:title="@string/application_title" />
    <!-- Behavior -->
    <header android:title="@string/behavior_title" />
    <!-- General -->
    <header android:title="@string/general_title" />
        <header
            android:id="@+id/backlight_settings"
            android:fragment="com.liquid.settings.activities.Backlight"
            android:title="@string/backlight_title"
            />
    <!-- Interface -->
    <header android:title="@string/ui_title" />
        <header
            android:id="@+id/power_widget_settings"
            android:fragment="com.liquid.settings.activities.PowerWidget"
            android:title="@string/title_widget_buttons"
            />
    <!-- Performance -->
    <header android:title="@string/performance_title" />
    <!-- Sound -->
    <header android:title="@string/sound_title" />
</preference-headers>
